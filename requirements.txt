# ---- PyTorch 2.8.0 + CUDA 12.8 系に最適化 ----
# PyTorchスタックは最初に手動インストール推奨:
# pip install torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0

# PyTorchスタック（参考用、実際は上記コマンドで先行インストール）
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0

# AI/ML ライブラリ（PyTorch 2.8.0対応版）
transformers==4.56.2
accelerate==1.10.1
datasets==4.1.1
tokenizers>=0.22.0,<0.23.0

# NVIDIA Nemotron-Nano-9B-v2対応
mamba-ssm==2.2.5

# Web フレームワーク
fastapi==0.115.0
uvicorn==0.30.6
sse-starlette==2.1.3
pydantic==2.9.2
pydantic-core==2.23.4
starlette==0.38.5

# Frontend
streamlit==1.38.0
requests==2.32.3

# 共通依存関係（PyTorch 2.8.0と互換性確認済み）
httpx==0.27.2
numpy==2.1.2
packaging==24.2
protobuf==5.27.5

# 必要に応じてインストール順序:
# 1. pip install torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0
# 2. pip install transformers accelerate datasets
# 3. pip install mamba-ssm==2.2.5
# 4. pip install -r requirements.txt（残りの依存関係）